// BANNERSTART
// Copyright: 2011, Galois, Inc.
// License-file: LICENSE
// Author: Iavor S. Diatchki (diatchki@galois.com)
// BANNEREND

/*@ lemma unsigned_and : \forall integer x; \forall integer y; 0 <= x && 0 <= y ==> 0 <= (x & y);
  @*/
/*@ lemma unsigned_and2 : \forall integer x; \forall integer y; 0 <= x && 0 <= y ==> (x & y) <= x && (x & y) <= y;
  @*/
